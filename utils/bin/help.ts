export const help = () => {
  return `
    --------------------------------------------------
    help: list out possible commands
    COMMANDS:
      help           list out possible commands
      clear          clear the screen
      neofetch       short summary of me
    --------------------------------------------------`;
}
